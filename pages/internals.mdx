import { Meta, NextPrevLink } from '../components';

export const meta = {
  title: 'Internals - Hardware Report',
};

# Internals

<NextPrevLink
  next={{
    href: 'connectors',
    label: 'Connectors',
  }}
  previous={{
    href: '/',
    label: 'Home',
  }}
/>

export default Meta(meta);

;
