import { Meta, NextPrevLink } from '../components';

export const meta = {
  title: 'Firmware - Hardware Report',
};

# Firmware

<NextPrevLink
  previous={{
    href: 'internals',
    label: 'Internals',
  }}
/>

export default Meta(meta);

;
