import { Meta, NextPrevLink } from '../components';

export const meta = {
  title: 'Connectors - Hardware Report',
};

# Connectors

<NextPrevLink
  next={{
    href: 'types',
    label: 'Types',
  }}
  previous={{
    href: 'internals',
    label: 'Internals',
  }}
/>

export default Meta(meta);

;
